spec: v0.6

name: ossu

unikraft:
  version: stable
  kconfig:
    CONFIG_LIBUKBUS: 'y'
    CONFIG_LIBPOSIX_FDIO: 'y'
    CONFIG_LIBUKNETDEV_EINFO_LIBPARAM: 'y'

targets: qemu/arm64

#libraries:
#  musl:
#    version: stable
  #openssl:
  #  version: stable
  #pthread-embedded:
  #  version: stable
